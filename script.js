console.log("Portfolio Loaded!");

document.getElementById('contact-form').addEventListener('submit', (e) => {
    e.preventDefault();
    alert("In the next step, we will connect this to a Serverless API!");
});
